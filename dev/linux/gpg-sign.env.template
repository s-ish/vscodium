# Copy to dev/linux/gpg-sign.env for local GPG signing (gitignored).
# Used when packaging with ./dev/build-grok-ade.sh -p

# ASCII-armored private key (export with: gpg --armor --export-secret-keys <key-id>)
GPG_PRIVATE_KEY=
GPG_PASSPHRASE=